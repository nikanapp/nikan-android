<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppBaseTheme" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:colorAccent">@color/myvideo_title_back</item>
        <item name="android:forceDarkAllowed">true</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="StartActivityTheme" parent="@style/AppTheme">
        <item name="android:windowBackground">@drawable/start_splash_shoufa</item>
    </style>

    <style name="AdvertisementTheme" parent="SplashTheme">
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="SplashTheme" parent="@style/AppBaseTheme">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowTranslucentNavigation">true</item>

    </style>
</resources>